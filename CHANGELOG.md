# CHANGELOG — GYM PRO

قاعده ثبت (طبق AGENTS.md): هر نسخه یک بخش کامل دارد — تغییرات کاربری، تغییرات کد
(شماره‌دار)، باگ‌های رفع‌شده، تست‌ها، و یک خط «این نسخه چه چیزی را عوض نکرد».

---

## نسخه ۱۱ — ۱۴۰۵/۰۶/۱۲ — لایه مدرن + حذف صدای گفتار

### تغییرات کاربری
- پس‌زمینه **aurora** متحرک (سه هاله سبز/لیمو با حرکت آرام) افزوده شد.
- انیمیشن ورود کارت‌ها (fade + rise) با رعایت `prefers-reduced-motion`.
- دکمه‌های اصلی گرادیان **زنده** (شیفت رنگ) دارند.
- **حذف کامل صدای گفتار**؛ کوچ فقط با **بوق WebAudio + لرزش** کار می‌کند
  (در شروع/توقف استراحت و پایان جلسه).

### تغییرات کد
- ۱) افزودن `.aurora` (ثابت، `z-index:-1`) + `@keyframes auroraMove`.
- ۲) `.stat/.week-card/.mus-wrap/.plan` → `animation: riseIn`.
- ۳) `.btn.primary` → گرادیان متحرک (`gradShift`).
- ۴) حذف `speak/announceSet/isFa/isEn/isFaEn/loadVoices/bestOf/pickVoice/
     fillVoiceSel` و متغیرهای `voiceOn/voicePref/voicesCache`.
- ۵) حذف دکمه `#coachVoice`، انتخاب‌گر `#coachVoiceSel`، راهنمای
     `#coachVoiceHint` و کلاس‌های CSS مربوطه.
- ۶) `APP_VERSION` → ۱۱، کش SW → `gympro-v11`.

### باگ‌های رفع‌شده
- —

### تست‌ها
- `t_v6` (کوچ، بدون صدا)، `t_v7` (تازه‌ها)، `t_v4` (تایمر/عضلات)، `t_wizard`
  — همه سبز؛ بررسی شد هیچ ارجاعی به `speak/announceSet/voiceOn/coachVoice`
  باقی نمانده.

### این نسخه چه چیزی را عوض نکرد
- منطق تمرین کوچ، موتور برنامه، نقشۀ عضلات، تایمر بوق/لرزش، PWA و صفحات
  روز دست‌نخورده ماندند.

---

## نسخه ۱۰ — ۱۴۰۵/۰۶/۱۲ — محدودیت صدا به فارسی/انگلیسی

### تغییرات کاربری
- انتخاب‌گر صدا فقط صداهای **فارسی و انگلیسی** را نشان می‌دهد.
- انتخاب خودکار: اولویّت فارسی، در غیاب آن انگلیسی.

### تغییرات کد
- ۱) `isFa/isEn/isFaEn` برای فیلتر زبان.
- ۲) `fillVoiceSel` فقط `isFaEn` را لیست می‌کند.
- ۳) `pickVoice` فارسی را ترجیح می‌دهد، وگرنه انگلیسی (نه زبان تصادفی).
- ۴) `APP_VERSION` → ۱۰، کش SW → `gympro-v10`.

### باگ‌های رفع‌شده
- —

### تست‌ها
- رگرسیون کامل (t_v6/v7/v4/wizard) سبز.

### این نسخه چه چیزی را عوض نکرد
- ظاهر و بقیه قابلیت‌ها.

---

## نسخه ۹ — ۱۴۰۵/۰۶/۱۲ — صدای فارسی

### تغییرات کاربری
- انتخاب خودکار باکیفیت‌ترین صدای فارسی دستگاه.
- انتخاب‌گر دستی صدا داخل کوچ (ذخیره‌شده) + راهنمای فعال‌سازی در سیستم.

### تغییرات کد
- ۱) `voiceschanged` برای بارگذاری صحیح لیست صداها.
- ۲) `pickVoice` با اولویّت کیفیت (Google/Natural/Neural).
- ۳) `#coachVoiceSel` + `gympro.voice.v1`.
- ۴) `APP_VERSION` → ۹، کش SW → `gympro-v9`.

### باگ‌های رفع‌شده
- رفع انتخاب اشتباه صدای انگلیسی به‌جای فارسی.

### تست‌ها
- رگرسیون کامل سبز.

### این نسخه چه چیزی را عوض نکرد
- منطق تمرین و بقیه UI.

---

## نسخه ۸ — ۱۴۰۵/۰۶/۱۲ — بازطراحی مدرن

### تغییرات کاربری
- گلس‌مورفیزم، هاله/گرید محیطی، دکمه‌های قرصی با لیفت، گرادیان روی عنوان‌ها/
  اعداد/تیک، درخشش لوگو، اسکرول‌بار سبز.

### تغییرات کد
- ۱) توکن‌های `--glass/--line2/--grad` در `:root`.
- ۲) پس‌زمینه بدنه با radial + grid.
- ۳) `.btn`/`.stat`/`.day-panel`/`.ex`/`.week-card`/`.wz`/`.plan` بازطراحی‌شده.
- ۴) `:focus-visible` و اسکرول‌بار سفارشی.
- ۵) `APP_VERSION` → ۸، کش SW → `gympro-v8`.

### باگ‌های رفع‌شده
- —

### تست‌ها
- رگرسیون کامل سبز (بدون تغییر کلاس/ID).

### این نسخه چه چیزی را عوض نکرد
- موتور برنامه، کوچ، نقشۀ عضلات، PWA.

---

## نسخه ۷ — ۱۴۰۵/۰۶/۱۲ — پنجره «چه تازه است»

### تغییرات کاربری
- بعد از هر بروزرسانی، پنجره خودکار قابلیت‌های جدید + تاریخ نسخه.
- دکمۀ «📣 تازه‌ها» در پاورقی برای تاریخچه کامل.

### تغییرات کد
- ۱) `CHANGELOG` درون‌برنامه‌ای + `APP_VERSION` + `gympro.seen.v1`.
- ۲) منطق بوت: `seen < APP_VERSION` → باز خودکار.
- ۳) کش SW → `gympro-v7`.

### باگ‌های رفع‌شده
- نصب تازه مزاحمت نمی‌کند؛ کاربر قدیمی بدون کلید → تاریخچۀ کامل.

### تست‌ها
- `t_v7` (۳ سناریو: بازگشته/نصب‌تازه/پاورقی) سبز.

### این نسخه چه چیزی را عوض نکرد
- بقیه قابلیت‌ها.

---

## نسخه ۶ — ۱۴۰۵/۰۶/۱۲ — دستیار گام‌به‌گام تمرین (کوچ)

### تغییرات کاربری
- دکمۀ «▶ حالت تمرین» → کوچ تمام‌صفحه ست‌به‌ست.
- گفتار فارسی آفلاین + حلقۀ استراحت بزرگ (+۱۵، رد استراحت).
- ثبت خودکار حرکت‌ها در پیشرفت/عضلات.

### تغییرات کد
- ۱) `#coach` overlay + `openCoach/closeCoach/coachSetDone/tickCoach/...`.
- ۲) `markExDone(dayId,idx)` (فقط علامت‌زدن).
- ۳) `body.coach-open` داک تایمر پایین را مخفی می‌کند.
- ۴) کش SW → `gympro-v6`.

### باگ‌های رفع‌شده
- نمایش فوری شمارش استراحت (`tickCoach()` بعد از render).

### تست‌ها
- `t_v6` (کل جلسه تا پایان) سبز.

### این نسخه چه چیزی را عوض نکرد
- موتور برنامه و نقشۀ عضلات.

---

## نسخه ۵ — ۱۴۰۵/۰۶/۱۲ — آیکون جدید

### تغییرات کاربری
- آیکون هالتر کروم + نبض لیمویی (معمولی + maskable).

### تغییرات کد
- ۱) بازطراحی `icons/icon.svg` و `icon-maskable.svg`.
- ۲) کش SW → `gympro-v5`.

### باگ‌های رفع‌شده
- —

### تست‌ها
- اعتبارسنجی SVG + رندر QuickLook + رگرسیون سبز.

### این نسخه چه چیزی را عوض نکرد
- همه منطق برنامه.

---

## نسخه ۴ — ۱۴۰۵/۰۶/۱۱ — تایمر استراحت + نقشۀ عضلات

### تغییرات کاربری
- تایمر خودکار هنگام تیک‌زدن (بوق + لرزش).
- نقشۀ حرارتی بدن (جلو/پشت) با وضعیت ریکاوری.

### تغییرات کد
- ۱) `#restDock` + `startRest/finishRest/beep/vibrate`.
- ۲) دو اندام SVG + `SLOT_M` + `MUSCLES` (مدل ریکاوری ساعتی).
- ۳) کش SW → `gympro-v4`.

### باگ‌های رفع‌شده
- —

### تست‌ها
- `t_v4` سبز.

### این نسخه چه چیزی را عوض نکرد
- ویزارد و موتور برنامه.

---

## نسخه ۳ — ۱۴۰۵/۰۶/۱۱ — PWA

### تغییرات کاربری
- نصب روی گوشی/دسکتاپ + آفلاین کامل.

### تغییرات کد
- ۱) `manifest.webmanifest` + `sw.js` (cache-first).
- ۲) دکمۀ نصب (`beforeinstallprompt`).

### باگ‌های رفع‌شده
- —

### تست‌ها
- سرو زنده (۵ فایل ۲۰۰) + رگرسیون سبز.

### این نسخه چه چیزی را عوض نکرد
- محتوای برنامه.

---

## نسخه ۲ — ۱۴۰۵/۰۶/۱۱ — ویزارد + برنامه‌های پیشنهادی

### تغییرات کاربری
- ۳ برنامه پیشنهادی با درصد انطباق و انتخاب آزاد.
- انتخاب روزهای واقعی هفته (۲ تا ۶ روز).

### تغییرات کد
- ۱) موتور `SLOTS/TPL/CANDS/SCHEME` + `candidates/pickPlan`.
- ۲) ویزارد ۴ گام.

### باگ‌های رفع‌شده
- محاسبۀ امتیاز با روزِ واقعی ویزارد (نه پیش‌فرض).

### تست‌ها
- `t_wizard` سبز.

### این نسخه چه چیزی را عوض نکرد
- —

---

## نسخه ۱ — ۱۴۰۵/۰۶/۱۱ — انتشار اولیه

### تغییرات کاربری
- سایت تک‌فایل آفلاین: اطلاعات بدنی، برنامۀ هفتگی، پیشرفت روز/هفته.
- تم تیره/سبز، کارت‌های اطلاعات بدنی، نوار پیشرفت.

### تغییرات کد
- ۱) `index.html` تک‌فایل (HTML+CSS+JS).
- ۲) `localStorage` برای ذخیرۀ داده‌ها.

### باگ‌های رفع‌شده
- —

### تست‌ها
- —

### این نسخه چه چیزی را عوض نکرد
- (اولین نسخه)
