# تاریخچه پروژه GymPro

## ۱) معرفی

**GYM PRO** — وب‌سایت آفلاین برنامه تمرینی ورزشی. تک‌فایلی، بدون فریمورک،
بدون هیچ منبع بیرونی (CDN/فونت/آیکون) — با دابل‌کلیک روی `index.html`
در هر مرورگری بدون اینترنت کار می‌کند.

## ۲-ت) نسخه ۴ — تایمر استراحت + نقشه حرارتی عضلات (۱۴۰۵/۰۶/۱۱)

- **تایمر استراحت:** داک پایین با حلقۀ شمارش SVG؛ **شروع خودکار** هنگام
  تیک‌زدن هر حرکت (بر زمان rest همان حرکت) + دکمۀ ⏱ روی هر ردیف (بدون
  تیک‌زدن، با stopPropagation) + +۱۵/⏭/✕ + **بوق سه‌نتی WebAudio** و
  `navigator.vibrate` (هر دو آفلاین). ادامه‌پذیر پس از رفرش با
  `gympro.rest.v1` (تاریخ پایان).
- **نقشۀ عضلات:** دو اندام SVG (جلو/پشت، ۲۳ ناحیۀ `data-m`) + `SLOT_M`
  (اسلات→عضله) + `MUSCLES` با مدل ریکاوری ساعتی (شکم/ساق ۲۴، بازو ۳۶،
  سینه/سرشانه/ران ۴۸، زیربغل/پشت‌ران/باسن ۶۰). رنگ: قرمز<۵۰٪، نارنجی<۱۰۰٪،
  لیمویی<۱۵۰٪، سبز+گلو ≥؛ بدون سابقه خاکستری. تیک حرکت → ثبت `Date.now()`
  در `gympro.muscles.v1`؛ کلیک روی عضله → خط جزئیات؛ تازه‌سازی خودکار ۶۰ث.
  ⚠️ برداشتن تیک، زمان عضله را برنمی‌گرداند (مستند).
- تست `tests/t_v4.js`: تایمر خودکار ۱:۳۰، ثبت عضله، +۱۵، اسکیپ→«برو!»،
  پاک‌شدن LS، کلیک عضله، ⏱ بدون تیک → همه سبز. رگرسیون t_wizard سبز.
- کش SW به `gympro-v4` ارتقا یافت.

## ۲-خ) نسخه ۸ — بازطراحی مدرن 💎 (۱۴۰۵/۰۶/۱۲)

- درخواست مهدی: «مدرنترش کن». بازطراحی ظاهر با زبان تصویری ۲۰۲۴:
  - **گلس‌مورفیزم**: همه کارت‌ها/پنل‌ها نیمه‌شفاف `rgba(255,255,255,.04)` با
    `backdrop-filter:blur` و هایلایت ۱px بالا (جدایی لایه‌ای).
  - **هاله‌های محیطی**: سه radial gradient سبز/لیمو در گوشه‌ها + **گرید ظریف**
    ۱px که با اسکرول ثابت می‌ماند.
  - **دکمه‌های قرصی** (border-radius 999px) با `translateY(-2px)` در هاور و
    `scale(.97)` در فشردگی + سایه رنگی گرادیانی برای primary.
  - **گرادیان لیمو→سبز** روی عنوان بخش‌ها، نام GYM PRO، اعداد حلقه پیشرفت،
    تیک‌های انجام‌شده و انتخاب‌های ویزارد؛ **درخشش متحرک** روی لوگو.
  - اسکرول‌بار باریک سبز، `:focus-visible` سبز، `::selection` روشن.
- بدون تغییر کلاس/ID → تست‌ها همچنان سبز. کش SW→`gympro-v8`، پاورقی نسخه ۸.

## ۲-ح) نسخه ۷ — پنجره «چه تازه است» (۱۴۰۵/۰۶/۱۲)

- درخواست مهدی: بعد از هر بروزرسانی، در اولین ورودِ کاربر، پنجره‌ای قابلیت‌های
  جدید + **تاریخ بروزرسانی** را نشان دهد.
- `CHANGELOG` درون‌برنامه‌ای (۷ نسخه، هرکدام: نسخه/تاریخ/عنوان/بولت‌ها) +
  `APP_VERSION=7` + `gympro.seen.v1` (آخرین نسخه دیده‌شده).
- منطق بوت: `seen < APP_VERSION` → باز خودکار با فیلتر «فقط نسخه‌های جدیدتر»
  (کارت اول با هاله سبز)؛ بستن (دکمه یا کلیک برون‌زد) → ذخیرۀ `seen`.
  **نصب تازه بدون برنامه → مزاحمت نمی‌شود** (فقط seen ذخیره)؛ کاربر قدیمیِ
  بدون کلید seen → تاریخچۀ کامل.
- دکمۀ **«📣 تازه‌ها و بروزرسانی‌ها»** در پاورقی → تاریخچۀ کامل ۷ نسخه.
- تست `tests/t_v7.js`: ۳ سناریو (بازگشته بعد از آپدیت / نصب تازه / دکمۀ
  پاورقی) — همه سبز. فیکس assert تست: کارت اول v7 است (مرتب‌سازی نزولی).
- کش SW→`gympro-v7`، پاورقی نسخه ۷.

## ۲-چ) نسخه ۶ — دستیار گام‌به‌گام تمرین (۱۴۰۵/۰۶/۱۲)

- دکمۀ **«▶ حالت تمرین»** در سربرگ هر روز تمرینی → کارت تمام‌صفحۀ کوچ:
  نام حرکت، «ست X از Y»، تکرار هدف، نقطه‌های ست هر حرکت (سبز=تمام)،
  نوار پیشرفت کل جلسه و شمارش «a/b ست».
- جریان: «✅ ست تمام» → استراحت با حلقۀ شمارش بزرگ (شروع فوری، +۱۵،
  ⏭ رد استراحت) → اتمام استراحت = اعلام و ورود خودکار به ست بعد →
  پایان حرکت = ثبت خودکار در `done` (تیک، عضله، رینگ هفتگی) → حرکت بعد →
  پایان جلسه = «تمرین کامل شد 🎉» + دکمۀ بازگشت.
- **گفتار فارسی آفلاین** با SpeechSynthesis (`fa-IR`، انتخاب صدای فارسی اگر
  موجود): اعلام حرکت/ست/تکرار در شروع، «آفرین! N ثانیه استراحت»، «وقت
  ست بعدی!»، «تبریک!». دکمۀ 🔊/🔇 + «تکرار» اعلام با کلیک مجدد.
- در حالت کوچ، داک تایمر پایین مخفی (`body.coach-open`) تا تداخل نشود؛
  بوق و لرزش همان `beep()/vibrate()` بخش ۱۳ استفاده می‌شود.
- `markExDone(dayId,idx)`: فقط علامت‌زدن (برخلاف تیکِ دستی، برداشتن ندارد)
  و همگام‌سازی عضلات/رینگ/پنل پشت سر.
- تست `tests/t_v6.js`: کل جلسه (۷ حرکت ۴ ست) تا «تمرین کامل شد»، ثبت همه
  در done، فازها، صدا، خروج. دو فیکس حین تست: نمایش فوری شمارش استراحت
  (`tickCoach()` بعد از render) و تصحیح assert تست.
- کش SW→`gympro-v6`، footer نسخه ۶. بکاپ
  `backup/gympro-v6-1405-06-12.{zip,bundle}` → منتشر شد: `f39d8e2`.

## ۲-ج) نسخه ۵ — آیکون جدید (۱۴۰۵/۰۶/۱۲)

- آیکون بازطراحی: پس‌زمینه تیره #101d16، هاله سبز شعاعی، **هالتر کروم**
  (میلۀ نقرهای با گرادیان + بشقاب‌های سبز دوسطحی)، **خط نبض قلب** لیمویی
  درخشان با گلو — هم‌خوان با برند GYM PRO و هویت سلامت/تمرین. نسخه
  `icon-maskable.svg` با پس‌زمینه تمام‌صفحه و محتوا در ناحیۀ امن ۸۰٪.
- تست/تأیید: صحت ساختار SVG + رندر QuickLook (اندازۀ فایل سالم) + هر دو
  تست jsdom سبز. کش SW→`gympro-v5`، footer نسخه ۵.
- **بکاپ (قبل از انتشار):** `backup/gympro-v5-1405-06-12.zip` +
  `gympro-v5-1405-06-12.bundle` (تأییدشده).
- **منتشر شد ✅:** کامیت `c7a3cc9` روی main (push با توکن مهدی).
  **GitHub Pages فعال شد و زنده است:** https://m38d1.github.io/GymPro/
  — index و icon هر دو ۲۰۰؛ از این آدرس PWA قابل نصب است.

## ۲-ث) انتشار گیت‌هاب (۱۴۰۵/۰۶/۱۱)

- **بکاپ (قاعده جدید AGENTS.md):** `backup/gympro-v4-1405-06-11.zip` (۱۲ فایل)
  + `backup/gympro-v4-1405-06-11.bundle` (تأیید‌شده).
- ریپوی محلی `main` با کامیت اولیه؛ ریموت `git@github.com:m38d1/GymPro.git`.
- `README.md` + `.gitignore` نوشته شد؛ HISTORY.md عمداً **خارج از ریپو** (خصوصی).
- کلید SSH تازه (`~/.ssh/id_ed25519`) ساخته شد (مسیر جایگزین، استفاده نشد).
- **منتشر شد ✅** با توکن fine-grained مهدی (بدون ذخیره در دستگاه):
  `main → c9e9022` روی https://github.com/m38d1/GymPro — README و index.html
  عمومی تأیید شدند (HTTP 200). توکن دسترسی Pages نداشت؛ Pages از پنل:
  Settings → Pages → Source: Deploy from branch → main / root → Save
  → آدرس نهایی: `https://m38d1.github.io/GymPro/` (از آنجا PWA نصب می‌شود).
- ⚠️ توکن در چت رد‌وبدل شده؛ پس از تأیید انتشار، revoke/rotate شود.

## ۲-پ) نسخه ۳ — PWA (۱۴۰۵/۰۶/۱۱)

- `manifest.webmanifest` (فارسی/RTL، standalone، تم تیره) +
  `icons/icon.svg` و `icons/icon-maskable.svg` (لوگوی هالتر روی گرادیان سبز).
- `sw.js`: سرویس‌ورکر کش‌اول — نصب `SHELL` (۵ فایل) در install، پاک‌سازی
  کش‌های قدیمی در activate، در fetch: کش ← شبکه ← در نهایت index.html
  (ناوبری آفلاین).
- `index.html`: تگ‌های متادیتای PWA در head، دکمۀ «📲 نصب اپ» (با
  `beforeinstallprompt` و پیام جایگزین برای iOS)، رجیستر SW فقط روی http(s)
  (روی `file://` لازم نیست — خود فایل آفلاین است).
- ⚠️ نکته: سرویس‌ورکر روی `file://` کار نمی‌کند؛ برای نصب PWA باید از
  http(s) سرو شود (GitHub Pages یا `python3 -m http.server` در شبکه محلی).
  اما استفاده آفلاین با دابل‌کلیک روی `index.html` از نسخه ۱ هم ممکن بود.
- تست: `node --check sw.js` + JSON معتبر + سرو زنده روی پورت ۸۷۳۱ (۵ فایل،
  همه ۲۰۰) + رگرسیون `t_wizard.js` سبز.

## ۲-ب) نسخه ۲ — پرسشنامه + موتور پیشنهاد برنامه (۱۴۰۵/۰۶/۱۱)

- ویزارد ۴ گامی (مودال تمام‌صفحه): ۱) جنسیت/سن/قد/وزن ۲) هدف (چربی‌سوزی/
  عضله‌سازی/قدرت/سلامت) + سابقه (مبتدی/متوسط/پیشرفته) ۳) تعداد روز تمرین (۲–۶)
  ۴) **سه برنامه پیشنهادی با درصد انطباق** — برترین با ⭐ مشخص می‌شود ولی
  انتخاب آزاد است.
- موتور برنامه‌سازی: بانک حرکت `SLOTS` (اسلات‌های سطح‌بندی‌شده lv1-3 → حرکت
  متناسب با سابقه انتخاب می‌شود)، ۱۵ قالب روز `TPL`، ۱۵ نامزد برنامه `CANDS`
  (۳ نامزد برای هر تعداد روز: فول‌بادی، UL، PPL، برواسپلیت، HIIT و ترکیبی).
- ست/تکرار/استراحت از `SCHEME` بر پایه هدف؛ مبتدی −۱ ست، پیشرفته +۱ ست روی
  حرکت اصلی. توزیع روزها در هفته با `PATTERNS` (جمعه همیشه استراحت).
- BMR ارتقا یافت به Mifflin-St Jeor با جنسیت/سن. چیپ برنامه فعال در سربرگ + دکمه
  «🧠 تغییر برنامه / پرسشنامه».
- ذخیره‌سازی تازه: `gympro.profile.v1`؛ تیک‌ها `gympro.done.v2` به شکل
  `{prog, map}` — با تغییر برنامه تیک‌ها ریست می‌شود.
- تست `tests/t_wizard.js` (jsdom): کل مسیر ویزارد → انتخاب برنامه دوم →
  تیک حرکت → localStorage. دو باگ واقعی گرفت: (۱) `PLAN` آبجکت به‌جای آرایه
  روزها ست می‌شد (۲) نامزدها با `profile.days` قدیمی محاسبه می‌شدند نه
  `wState.days` تازه ویزارد. هر دو رفع → تست سبز.
- **انتخاب روزهای هفته (همان نشست):** گام ۳ از «تعداد روز» به هفت دکمۀ
  چندانتخابی شنبه←جمعه تبدیل شد (۲ تا ۶ روز، شمارنده زنده). `profile.dayIdx`
  اضافه شد؛ `buildPlan` دقیقاً روی روزهای انتخابی می‌چیند و چیپ برنامه‌ها
  همان روزها را سبز نشان می‌دهد. مهاجرت پروفایل قدیمی: `dayIdx = PATTERNS[days]`.
  تست: پاک‌کردن پیش‌انتخاب‌ها → انتخاب ۴ روز → نامزدهای ۴روزه + چیدمان درست.
- ⚠️ نکته jsdom: برای localStorage باید `url:"https://..."` به JSDOM داده شود
  (about:blank → SecurityError).

## ۲) ویژگی‌ها (نسخه ۱ — ۱۴۰۵/۰۶/۱۱)

- 🌙 تم کاملاً تیره + پالت سبز ورزشی (`#22c55e` → `#a3e635`)، گلو و گرادیان.
- ⚡ سربرگ چسبان (sticky + blur) با برند **GYM PRO** و لوگوی هالتر SVG اینلاین.
- 📊 ۶ کارت اطلاعات بدنی: وزن، قد، BMI (خودمحاسب)، چربی بدن، ضربان استراحت،
  کالری روزانه + BMR (Harris-Benedict). فرم ویرایش با ذخیره در `localStorage`.
- 📅 ۷ تب روزهای هفته (شنبه←جمعه) با نشانگر «امروز» و نقطه وضعیت
  (خاکستری/زرد/سبز) روی هر تب.
- 🏋️ برنامه تمرینی ۴ روز تمرینی + ۳ روز استراحت؛ هر حرکت با ست×تکرار
  و زمان استراحت؛ کلیک/اینتر = تیک انجام.
- 📈 نوار پیشرفت روز (با انیمیشن برق) + شمارش ست‌ها + حلقه پیشرفت هفتگی (SVG ring)
  + نوارهای مینی هر روز + Toast تبریک در تکمیل روز.
- ↺ دکمه «ریست امروز» در سربرگ.
- 📱 ریسپانسیو: گرید auto-fit کارت‌ها، تب‌های اسکرول‌افقی، بریک‌پوینت ۷۲۰/۳۸۰px.

## ۳) ساختار فنی

- تک‌فایل `index.html` (~۶۹۰ خط): CSS متغیرمحور + یک بلاک `<script>` ساده.
- داده برنامه: ثابت `DAYS` (آرایه ۷ روز، هر روز `ex:[{n,s,r,rest}]`).
- کلیدهای localStorage: `gympro.body.v1` و `gympro.done.v1`.
- اعداد با `toLocaleString("fa-IR")` فارسی می‌شوند.

## ۴) تست

- `node --check` روی اسکریپت استخراجی: OK.
- اسموک‌تست jsdom (با node_modules پروژه پروژه‌بان): بدون خطا —
  ۶ کارت، ۷ تب، تیک حرکت → `done-OK`، درصد روز و رینگ هفتگی درست.

## ۵) ایده‌های بعدی

- تایمر استراحت بین ست‌ها (بوق آفلاین با WebAudio).
- ثبت وزن دوره‌ای + نمودار روند (SVG خطی).
- چند برنامه (حجمی/کات/قدرتی) با سوییچ.
- خروجی/ورودی JSON برای انتقال بین دستگاه‌ها.
